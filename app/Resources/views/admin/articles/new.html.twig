{% extends 'admin/admin.html.twig' %}

{% block body %}
    <div class="content">
        <div class="content-form">
        <h1>Article creation</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('admin_articles_index') }}">Back to the list</a>
            </li>
        </ul>
    </div>
    </div>
{% endblock %}
